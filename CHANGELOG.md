# Changelog

Tất cả thay đổi đáng chú ý của `@xorcloud/stack-ai-chat-sdk` được ghi lại ở đây.

Định dạng dựa theo [Keep a Changelog](https://keepachangelog.com/), versioning theo [SemVer](https://semver.org/).

> **Vì sao file này bắt đầu từ hôm nay:** repo có 15 bản đã publish lên npm (`0.1.0` → `0.13.0`, xem `git tag` / `npm view @xorcloud/stack-ai-chat-sdk versions --json`) nhưng **chưa từng có CHANGELOG**. Đây là kênh duy nhất SDK có để báo cho người tích hợp biết bản mới sửa gì — trước đây không có kênh nào cả (không telemetry, không danh sách khách hàng, xem phần "Ta có biết ai đang dùng SDK" trong `III. TCH - Architecture Chat SDK` §6.1).
>
> Lịch sử trước ngày hôm nay **không được ghi lại hồi tố** ở đây — không phải mọi bản cũ đều có git tag tương ứng (`0.3.0, 0.4.0, 0.8.0, 0.9.0, 0.10.x, 0.11.0, 0.12.0` không có tag), nên viết lại chi tiết từng bản sẽ phải suy đoán một phần. Ai cần lịch sử đầy đủ, tra `git log --oneline` trực tiếp trong repo — đó là nguồn chính xác, tài liệu này không lặp lại để tránh lệch.

## [Unreleased]

Các thay đổi dưới đây **đã có trong code, CHƯA publish lên npm** (task đo/sửa nội bộ, chờ duyệt trước khi release — xem `work:6a7dd1f1f923a3035c710240`).

### Fixed
- **`StackAIChat.init()` và `updateConfig()` giờ `throw` ngay lập tức nếu thiếu hoặc `wsUrl` không phải URL hợp lệ**, kèm thông báo lỗi nói rõ vấn đề. Trước đây: thiếu `wsUrl` khiến `resolveSocketParams()` âm thầm rơi vào nhánh fallback, `io(undefined, ...)` **âm thầm thử kết nối tới origin của chính trang đang nhúng SDK** — người tích hợp chỉ thấy một `connect_error` khó hiểu, không biết nguyên nhân là thiếu cấu hình. (Cùng lớp lỗi với BUG-044 — cấu hình sai không kêu lên.)
- **`connect()` nội bộ (`useSocket.ts`) có thêm lớp kiểm tra `wsUrl` phòng vệ thứ 2** ngay trước khi gọi `io(...)`, phòng trường hợp giá trị `wsUrl` invalid lọt qua (ví dụ code JS thuần không qua kiểm tra kiểu của TypeScript).

### Changed
- **`config.onError` giờ có fallback `console.error`**: trước đây nếu người tích hợp không truyền `onError` trong config, mọi lỗi kết nối/gửi tin (`connect_error`, `disconnect` bất thường, `message:error`, gửi tin thất bại...) **biến mất hoàn toàn** — kể cả trong devtools của chính người tích hợp. Giờ nếu không có `onError`, SDK tự `console.error('[SDKChat]', ...)` để luôn có dấu vết debug được.

### Security
- **⚠️ ĐÍNH CHÍNH (2026-08-13, task `6a7e1952973d78cf77ae31c1`): dòng "không có bản nào từng phát hành bị ảnh hưởng" bên dưới là SAI.** Đã kiểm tra ngược 15 tarball npm thật (`npm pack @xorcloud/stack-ai-chat-sdk@<mỗi bản>`, đọc `package/dist/index.js`): **`v0.12.2` và `v0.13.0` (bản mới nhất hiện có trên npm) CÓ đưa `err.context.responseText` (chưa che) vào `detail.httpResponse` rồi gọi `onError(message, detail)`** — logic giống hệt bản đang sửa, không có `redactToken()` hay bất kỳ cơ chế che nào (`grep "REDACTED\|redactToken"` = 0 kết quả ở cả hai bản). Đã đo lại bằng chính harness gateway-giả, dùng nguyên văn dòng handler minified copy từ tarball `0.12.2`: token lộ nguyên vẹn trong output `onError()` — `{"message":"xhr poll error","detail":{"type":"TransportError","httpStatus":400,"httpResponse":"...token=REAL_CUSTOMER_TOKEN_xyz789&EIO=4..."}}`.
  **`v0.1.0` → `v0.12.0` (13 bản, kể cả 0.1.1/0.3.0/0.4.0/0.8.0/0.9.0/0.10.x/0.11.x) KHÔNG bị ảnh hưởng** — đọc source xác nhận `connect_error` handler ở các bản này gọi `onError(message)` với **đúng 1 tham số**, không có `detail`, không có `context`/`responseText` nào được truyền ra ngoài SDK.
  **Điều kiện để token thực sự lộ ra ngoài (cả 3 phải đúng cùng lúc):** (1) app khách hàng đang chạy `0.12.2` hoặc `0.13.0`; (2) app đó có truyền `config.onError`; (3) implementation `onError` của app đó tự log/hiển thị/gửi đi tham số `detail` nhận được (nếu chỉ dùng `message`, không đọc `detail`, thì không lộ gì thêm). Ngoài ra cần gateway/proxy của hạ tầng đang echo request URL vào response body lỗi.
- **🔴 CẬP NHẬT (2026-08-13, task `6a7e1bedf923a3035c716b6d`): điều kiện thứ 4 ở trên KHÔNG còn `KHÔNG RÕ` — đã đo trực tiếp trên 2 gateway thật bằng canary token (`CANARY_NOT_A_REAL_TOKEN_xxxx`, không phải token thật), qua nhiều tầng lỗi khác nhau (wrong-namespace, sai `EIO`, thiếu `transport`, URL rác). Kết quả **lệch mong đợi ở một trong hai gateway** — ghi thẳng: **`skt.x-or.cloud` (máy `103.252.72.167`, hạ tầng bên ngoài, không ai trong dự án đứng tên quản trị) CÓ echo token vào body lỗi**; **`xsai-ws.x-or.cloud` (hạ tầng của ta, cả staging lẫn production) KHÔNG echo ở bất kỳ tầng lỗi nào đã thử.**

  | Gateway | Tầng lỗi | HTTP | Echo token? | Body (nguyên văn, rút gọn) |
  |---|---|---|---|---|
  | `xsai-ws.x-or.cloud` (staging) | backend down (namespace đúng) | 502 | KHÔNG | trang tĩnh nginx, không có nội dung request |
  | `xsai-ws.x-or.cloud` (staging) | namespace sai | 404 | KHÔNG | trang tĩnh nginx |
  | `xsai-ws.x-or.cloud` (staging) | `EIO=3` (sai protocol) | 502 | KHÔNG | trang tĩnh nginx (backend down nên không tới được tầng Engine.IO) |
  | `xsai-ws.x-or.cloud` (staging) | thiếu `transport` | 502 | KHÔNG | trang tĩnh nginx |
  | `xsai-ws.x-or.cloud` (staging) | URL rác/traversal | 404 | KHÔNG | trang tĩnh nginx |
  | `xsai-ws.x-or.cloud` (production) | sai mount point (thiếu `/chat`) | 404 | KHÔNG | trang tĩnh nginx — xác nhận lại BUG-047 |
  | `xsai-ws.x-or.cloud` (production) | namespace sai (có `/chat`) | 404 | KHÔNG | trang tĩnh nginx |
  | `xsai-ws.x-or.cloud` (production) | `EIO=3` trên path đúng | 400 | KHÔNG | `{"code":5,"message":"Unsupported protocol version"}` — JSON có cấu trúc nhưng không chứa URL/token |
  | `skt.x-or.cloud` | namespace sai | 404 | **CÓ** | `{"statusCode":404,"message":"Cannot GET /khong-ton-tai/socket.io/?token=CANARY_NOT_A_REAL_TOKEN_0008&EIO=4&transport=polling",...,"path":"/khong-ton-tai/socket.io/?token=CANARY_NOT_A_REAL_TOKEN_0008&EIO=4&transport=polling",...}` — cả `message` lẫn `path` đều mang nguyên token |

  **Kết luận:** lỗ hổng không còn "lý thuyết" — nó **đã được xác nhận có thật** trên `skt.x-or.cloud`, gateway `x-powered-by: Express` (khác hẳn kiến trúc nginx-tĩnh của `xsai-ws.x-or.cloud`). Trên hạ tầng `xsai-ws.x-or.cloud` mà ta quản trị, chưa đo được tầng nào echo — nhưng đây là danh sách tầng đã thử, **không phải chứng minh không tồn tại tầng nào khác echo** (ví dụ: lỗi 5xx từ backend NestJS thật, khác với 502 "backend down" đo được ở đây, chưa thử được vì không có quyền tắt bật backend thật).
  ⚠️ **Lưu ý quan trọng, chưa xác định được rõ trong phạm vi task này (`KHÔNG RÕ`):** `skt.x-or.cloud` là URL **ví dụ trong chính `CLAUDE.md` của repo này** (`wss://skt.x-or.cloud/ws/chat`) — không có gì đảm bảo đây chỉ là ví dụ lịch sử; do SDK không có telemetry (xem §6.1 `III. TCH - Architecture Chat SDK`), **ta không biết bao nhiêu khách hàng thật đang trỏ `wsUrl` vào `skt.x-or.cloud` thay vì `xsai-ws.x-or.cloud`**. Nếu tỷ lệ đó không nhỏ, gateway có-echo lại chính là gateway nhiều khách hàng thật đang dùng — không phải một góc khuất hiếm gặp.
- **Cửa sổ phơi nhiễm (task `6a7e1bedf923a3035c716b6d`, `npm view @xorcloud/stack-ai-chat-sdk time --json`, đo ngày 2026-08-13):** `0.12.2` publish `2026-05-22T04:47:58.515Z` → **83 ngày** đã trôi qua tính đến hôm nay; `0.13.0` (bản mới nhất) publish `2026-07-05T17:23:12.479Z` → **39 ngày**. Vì `0.12.2` và `0.13.0` cùng dính lỗi (không phải lỗi mới xuất hiện ở `0.13.0`), **lỗ hổng đã tồn tại liên tục trên bản mới nhất published trong 83 ngày**, không phải "2 tuần" và cũng không phải "8 tháng" — con số thật nằm giữa, gần 3 tháng.
- **Về giả thuyết proxy MITM doanh nghiệp của indra (§2.2, chưa có phép đo MITM thật):** giả thuyết **ĐỨNG**, không đổ — nhưng cần tách rõ 2 phần đã hỏi:
  1. *"`responseText` chỉ có nội dung khi có response HTTP hợp lệ?"* → **XÁC NHẬN ĐÚNG**, không cần đoán: cả 11 lượt `curl` ở trên (200/400/404/502) đều cho thấy: có response HTTP hoàn chỉnh → có body; đây là hành vi nền tảng của HTTP/XHR (`responseText` được điền từ bất kỳ response nào trình duyệt nhận được, bất kể ai là người trả lời), không riêng gì MITM. Ngược lại, nếu TLS đổ vỡ (không có CA nào được trust) thì trình duyệt không hoàn tất được handshake, không có response, `responseText` rỗng — đúng như điều kiện làm yếu mà indra tự nêu.
  2. *"MITM proxy doanh nghiệp có thực sự trả body cho request XHR polling không?"* → **CHƯA đo được trực tiếp** (không có proxy MITM thật để test trong phạm vi task) — vẫn ở mức `[SUY LUẬN]`, đúng như indra tự dán nhãn. Nhưng phát hiện `skt.x-or.cloud` ở trên là **một trường hợp thật, không phải MITM nhưng cùng lớp cơ chế**: một thực thể chặn HTTP nằm giữa client và đích cuối, không phải hạ tầng do ta hoặc khách hàng kiểm soát trực tiếp, trả về body có echo URL. Điều này không chứng minh MITM cụ thể, nhưng chứng minh **lớp rủi ro mà indra mô tả là có thật trong ít nhất một biến thể**, nên không có lý do để hạ giả thuyết MITM xuống mức "không đáng xem xét".
  **Chỗ indra suy diễn quá (lần 6):** lần này **không có** — điều kiện làm yếu indra tự nêu (không MITM → vỡ TLS → không có `responseText`) đo được là đúng, và nhãn `[SUY LUẬN]` tự gắn là chính xác, không cần hạ. Nếu có gì cần chỉnh, đó là hướng ngược lại: khung "gateway thật" trong các task trước (kể cả của chính tôi ở bản ĐÍNH CHÍNH bên trên) gộp chung `skt.x-or.cloud` và `xsai-ws.x-or.cloud` như thể một cặp tương đương — đo thật cho thấy **hai hạ tầng khác kiến trúc, khác hành vi hoàn toàn**, và gateway duy nhất được xác nhận có echo lại là gateway **ngoài quyền quản trị của dự án**, đồng thời (theo lưu ý `KHÔNG RÕ` ở trên) có thể vẫn là gateway mặc định trong tài liệu nội bộ. Đây không phải lỗi của indra riêng — là lỗi khung chung mà task này mới tách ra được.
- **🔴 PHẠM VI THẬT SỰ RỘNG HƠN (2026-08-13, task `6a7e1e8bf923a3035c7172fe`): việc echo vào `detail` chỉ là MỘT trong nhiều đường token có thể lộ — không phải đường duy nhất, và không phải đường lâu đời nhất.** Đo trực tiếp bằng harness cục bộ (bắt request thật gửi lên một server quan sát) và bằng kết nối thật tới `xsai-ws.x-or.cloud`: **token nằm trong QUERY STRING của mọi request handshake** (`io(origin, { query: { token }, auth: { token } })` ở `useSocket.ts`), với CẢ hai transport (`polling` lẫn `websocket` — kể cả khi ép `transports: ['websocket']`, URL upgrade vẫn mang `?token=...`). Xác nhận qua `npm pack` 8/8 bản kiểm tra (`0.1.0, 0.4.0, 0.8.0, 0.10.0, 0.11.0, 0.12.0, 0.12.2, 0.13.0`): dòng `query: { token: ... }` có mặt ở **TẤT CẢ**, kể cả bản đầu tiên `v0.1.0`. Nghĩa là: token đã nằm trong URL của **mọi kết nối, thành công lẫn thất bại**, từ ngày SDK ra đời — không phải chỉ khi lỗi, không phải chỉ từ `v0.12.2`.

  | Đường rò | Trạng thái | Phạm vi | Tồn tại từ bản nào | Vá ở đâu |
  |---|---|---|---|---|
  | Token trong query string của URL handshake | **ĐÃ KIỂM CHỨNG** (đo trực tiếp) | Mọi kết nối | `v0.1.0` | SDK: bỏ `query:{token}`, chỉ dùng `auth` — **cần backend xác nhận trước**, xem câu hỏi cho sten |
  | Log của `skt.x-or.cloud` (`.167`, ngoài quản trị dự án) ghi lại query string đó | `[SUY LUẬN mạnh]` — không có quyền đọc log máy đó | Mọi kết nối tới gateway đó | Từ khi máy nhận traffic — `KHÔNG RÕ` | Không vá được ở SDK |
  | Log/proxy trên `xsai-ws.x-or.cloud` (hạ tầng của ta) | `[SUY LUẬN mạnh]`, cùng cơ chế nhưng hạ tầng ta quản trị được | Mọi kết nối | `v0.1.0` | Có thể vá độc lập ở tầng nginx/log, không cần chờ SDK |
  | Body lỗi echo URL khi gateway lỗi (đã vá `redactToken()`) | **ĐÃ KIỂM CHỨNG** riêng `skt.x-or.cloud`; `xsai-ws.x-or.cloud` không echo ở các tầng đã thử | Chỉ khi lỗi | `v0.12.2`+ (chỉ bản có `detail`) | Đã vá ở SDK |
  | Session-replay/error-tracking bên thứ ba (Sentry, LogRocket, RUM...) nếu khách hàng tự cài trên trang của họ, thường ghi lại URL request XHR/WS kèm query string | `[SUY LUẬN]`, phát hiện phụ ngoài yêu cầu task, chưa đo | Mọi kết nối, tuỳ công cụ khách hàng cài | `v0.1.0` | Không vá được ở SDK — thuộc lựa chọn của khách hàng |

  **Về giả thuyết "Referer header" mà task này nêu:** KHÔNG đúng như mô tả — `Referer` của một request mang URL của TRANG gọi request đó, không phải ngược lại; URL socket.io (có token) không tự trở thành `Referer` của request khác. Bác lại điểm này.
  **Khả năng chuyển sang `auth`-only:** đo được bằng harness cục bộ — nếu CHỈ dùng `auth` (bỏ `query`), token **không xuất hiện trên URL ở cả polling lẫn websocket** (đo trực tiếp, không suy luận). Nhưng thử kết nối thật với `xsai-ws.x-or.cloud` production bằng 4 biến thể (`auth`+`query`, chỉ `auth`, chỉ `query`, không có gì) đều cho **kết quả giống hệt nhau** (`connect` rồi bị server disconnect `io server disconnect`) — nghĩa là không thể kết luận từ phía client server đọc token ở đâu. **`KHÔNG RÕ` — cần `sten` (AIWM) xác nhận `handshake.auth.token` hay `handshake.query.token` mới là nơi backend thực sự đọc**, trước khi SDK được phép bỏ `query`.
  **Ưu tiên cho anh Dũng:** vá `detail`/`redactToken()` (đã xong) vẫn nên giữ — rẻ, không rủi ro. Nhưng nó KHÔNG phải ưu tiên cao nhất: bề mặt query-string rộng hơn nhiều (mọi kết nối, mọi bản từ `v0.1.0`) và cũ hơn nhiều so với lỗi `detail` (chỉ 2 bản, chỉ khi lỗi). SDK không tự vá được bề mặt này một mình vì cần xác nhận backend trước — đây là việc liên service, không phải việc có thể đóng trong repo SDK.
- **Che token khỏi mọi thông báo lỗi (`reportError()` trong `useSocket.ts`) trước khi in ra `console.error` hoặc gọi `config.onError`.** Phát hiện qua đo thật: khi gateway/proxy trả lỗi HTTP mà echo lại request URL trong response body, `err.context.responseText` của `connect_error` chứa nguyên query string — bao gồm `?token=...`. Đã thêm `redactToken()`, thay mọi occurrence của token hiện tại bằng `[REDACTED]` trong cả `message` và `detail` (đệ quy qua object/array) trước khi log/callback — áp dụng cho toàn bộ 6 điểm gọi lỗi.
- **Đo lại `redactToken()` với 4 dạng token thực tế hơn (task `6a7e157b973d78cf77ae271b`) — phát hiện thêm 2 khoảng hở, cả hai đã vá:**
  1. **Token cũ sau `updateToken()`**: `query` của Engine.IO được chốt tĩnh lúc `io()` khởi tạo, không được rebuild khi `updateToken()` gọi `socket.disconnect().connect()` trên cùng socket — một lỗi xảy ra ngay sau refresh token vẫn có thể mang token **CŨ** trong query, trong khi `redactToken()` trước đây chỉ biết token **hiện tại** → token cũ lộ nguyên vẹn. Vá bằng cách redact theo **lịch sử tối đa 5 token gần nhất** (`tokenHistoryRef`), không chỉ token hiện tại.
  2. **Token bị cắt ngang trước khi redact**: `detail.httpResponse` từng bị `.slice(0, 500)` **trước** khi đưa vào `redactToken()` — nếu mốc cắt rơi giữa token, so khớp nguyên chuỗi thất bại và một đoạn dài (đo được 20+ ký tự liên tiếp) của token lộ ở dạng plaintext. Vá bằng cách đảo thứ tự: redact **trước**, cắt-để-hiển-thị (`truncateForDisplay()`, vẫn giới hạn 500 ký tự) **sau**.
  - Giới hạn còn lại (không vá được, vì không kiểm soát được): nếu **chính proxy/gateway** cắt ngắn response body trước khi tới trình duyệt, SDK chỉ thấy phần đã bị cắt — không có gì để đo lường mức độ an toàn ngoài "phần token nào lọt vào response thì có cơ hội được redact nếu còn nguyên vẹn, không phụ thuộc SDK".

## Trước khi có CHANGELOG

15 bản đã publish: `0.1.0` (2026-03-12, bản đầu tiên) → `0.13.0` (2026-07-05, bản hiện tại mới nhất). Chi tiết: `git log --oneline` hoặc `npm view @xorcloud/stack-ai-chat-sdk time --json`.
